# 📔 Core Verbs - 개발 일지 (Development Log)

이 개발 일지는 **Core Verbs** 영어 문장 암기 및 Spaced Repetition 학습 프로그램의 기획, 아키텍처 개편, 기능 고도화, 그리고 크로스 플랫폼 디버깅의 전 과정을 투명하게 기록한 역사입니다.

---

## 📅 프로젝트 개요
*   **프로젝트명**: Core Verbs 영어 문장 암기 앱
*   **핵심 목적**: 망각곡선 기반의 간격 반복 학습(SRS) 알고리즘을 이용해, 동사 중심의 원어민 영어 문장들을 자연스럽게 체화하고 리스닝/스피킹 억양(Intonation)을 정밀하게 익히는 웹 어플리케이션.
*   **주요 기술**: HTML5, Vanilla CSS3, Vanilla JS, Web Speech API (SpeechSynthesis), localStorage, Google Sheets API (TSV 동적 페칭).

---

## 🛠️ 개발 이력 및 버전 로그

### 📌 v1.0.0 — 초창기 프로토타입 (단일 파일 단계)
*   **개발 목표**: 구글 스프레드시트 데이터 연동 및 Leitner 스케줄러 핵심 로직 구현.
*   **구현 성과**:
    *   구글 스프레드시트 배포 주소(TSV)를 파싱하여 최신 단어 데이터를 실시간 수집.
    *   `localStorage`를 활용해 간격 반복 학습(SRS) 스케줄러 설계 (새 문장, 집중 복습, 단기 복습, 장기 복습 분류 배분).
    *   플래시 카드 레이아웃 구현 및 원어민 TTS 자동 읽기 연동.
*   **한계점**:
    *   HTML 단일 파일 내부(`index.html`)에 550줄이 넘는 마크업, CSS, JS 로직이 병합되어 가독성이 떨어짐.
    *   기본 TTS 엔진이 시스템 기본값(예: Microsoft David Desktop)에 의존하여 로봇처럼 건조하고 평평한 발음(인토네이션 부재) 유발.
    *   추후 통계 그래프, 오프라인 모드(PWA), 스피킹 평가 등으로의 확장성이 매우 취약한 구조.

---

### 📌 v2.0.0 — 3레이어 모듈 아키텍처 전환
*   **개발 일자**: 2026-05-12
*   **개발 목표**: 유지보수성과 확장성을 위해 HTML, CSS, JS를 완벽 분리하고, 가벼운 실행성(더블 클릭 로컬 실행) 유지.
*   **아키텍처 대개편**:
    *   `index.html` (구조): 중복 스크립트와 인라인 스타일을 전면 제거하고 외부 리소스를 연결하는 순수 뼈대로 정돈.
    *   `style.css` (디자인): 디자인 시스템 토큰(CSS variables)을 선언하여 일관된 톤앤매너 유지 및 Glassmorphism 설정창 디자인 반영.
    *   `app.js` (로직): 데이터 페칭, 학습 상태 연동, 오디오 재생 로직을 완전히 격리하여 독립적인 수정이 가능하도록 모듈화.
*   **이점**: 각 소스 파일이 100~200줄 내외로 압축되어 가독성이 300% 이상 향상되었으며, 오류 발생 시 추적이 극도로 단순해짐.

---

### 📌 v2.1.0 — 원어민 리스닝 엔진 & 억양(Intonation) 고도화
*   **개발 일자**: 2026-05-12
*   **개발 목표**: 기계적인 TTS 억양을 제거하고, 실제 원어민의 살아있는 억양을 다각도로 분석하며 들을 수 있는 환경 구축.
*   **구현 성과**:
    *   **스마트 추천 매칭 알고리즘**: 브라우저 로딩 음성들을 전수 분석하여 `Microsoft Edge Online (Natural)`, `Google Online`, `Apple Siri/Enhanced` 등 고품질 Neural 온라인 음성 소스를 최우선으로 순위 정렬 및 자동 매칭.
    *   **⚙️ 우측 슬라이딩 설정 패널**: 화면 우측 상단 기어(⚙️) 아이콘 탑재. 반투명 배경 블러 및 미려한 드로어 효과를 주어 언제든 리스닝 환경을 바꿀 수 있는 오버레이 패널 구현.
    *   **세밀한 배속 컨트롤**: `0.5x`에서 `1.5x`까지 `0.1x` 단위의 발음 속도 슬라이더 추가. 연음과 억양의 흐름을 정밀 추적 청음할 수 있도록 지원.
    *   **영구 동기화**: 학습자의 오디오 설정은 기존의 `localStorage` 메모리 스키마 내부의 `settings` 하위 객체로 완전 통합되어 영구 보존.

---

### 📌 v2.2.0 — iOS/Safari 렌더링 디버깅 및 한글 가이드 통합
*   **개발 일자**: 2026-05-12
*   **개발 목표**: 아이폰(iOS Chrome/Safari)에서 불필요한 시스템 효과음이 노출되는 크로스 플랫폼 버그 해결 및 가독성 개선.
*   **기술적 발견 및 디버깅**:
    *   *현상*: 컴퓨터 크롬은 깔끔하게 6개의 구글/MS 목소리만 나오나, 아이폰 크롬에서는 기괴한 벨소리(`Bells`, `Cellos`), 장난형 목소리(`Good News`, `Bad News`, `Whisper`) 등 수십 가지 잡음이 드롭다운에 도배됨.
    *   *원인 분석*: 
        1. iOS용 크롬은 껍데기만 구글이며, 내부적으로는 Apple의 `WebKit` 엔진을 100% 경유하여 실행됨.
        2. Apple은 장애인 접근성 사운드 모델(Bells, Good News 등)의 기저 언어를 미국 영어(`en-US`)로 분류하여 제공함. 이 때문에 브라우저가 영어 목소리를 가져올 때 오디오 효과음까지 정규 목소리로 잘못 끌어오는 현상 발생.
    *   *해결책*: `noveltyVoices` 예외 목록 필터를 정의하여, 학습에 불필요한 벨소리 및 오작동 소리들을 매칭 점수 매기기 단계에서 원천 필터 아웃(`return -1`) 처리 완료. 아이폰에서도 오직 실제 인간 목소리(Samantha, Siri 등)만 아름답게 노출되도록 차단 성공.
*   **한글 억양 가이드 가독성 패치**:
    *   기계 중심적인 목소리 이름(예: `Microsoft Aria Online`) 대신 직관적인 한글과 이모지 국기, 뉘앙스 스타일 설명문을 추가 제공하도록 맵핑 포맷터(`getFriendlyVoiceLabel`) 탑재.
    *   예: `🇺🇸 [미국 여성] Aria - 지적이고 신뢰감 넘치는 뉴스 아나운서 톤 (강력 추천 🌟)`
    *   예: `🇬🇧 [영국 여성] Sonia - 고급스럽고 우아한 클래식 영국 귀족풍 악센트 🌟`

---

## 📈 향후 개발 로드맵 (Future Roadmap)

1.  **🎙️ 실시간 스피킹 발음 평가 시스템**:
    *   Web Speech API의 `SpeechRecognition`을 이용하여 학습자가 직접 말하는 영어를 실시간 감지하고 텍스트 일치율을 계산해 스피킹 피드백 제공.
2.  **📊 주간 학습 차트 대시보드**:
    *   `localStorage`에 누적 학습 날짜 및 암기 완료 문장 데이터를 날짜별 통계로 기록하고, 가벼운 라이브러리(또는 순수 SVG Canvas)를 사용하여 예쁜 그래프 시각화.
3.  **📦 Progressive Web App (PWA) 탑재**:
    *   `manifest.json` 및 `service-worker.js`를 연결하여, 모바일 환경에서 별도 앱스토어 설치 없이 홈 화면에 바로가기 앱 아이콘으로 추가하여 실제 앱처럼 동작 가능하도록 구축.
4.  **💾 데이터 오프라인 캐싱**:
    *   구글 스프레드시트가 로딩되지 않는 비행기 내부, 지하 등의 인터넷 단절 환경(Offline)에서도 마지막 로컬 데이터로 중단 없이 학습을 이어갈 수 있는 오프라인 캐시 레이어 설계.
5.  **🤖 질문 노트 AI 자동 튜터링 및 파생 문장 생성**:
    *   질문 노트에 작성된 사용자의 질문(헷갈리는 문법, 뉘앙스 차이 등) 로그를 축적하고 AI가 즉각적인 답변을 제공하는 기능.
    *   자주 실수하거나 어려워하는 패턴을 분석하여, 이를 체화할 수 있는 '새로운 유사 파생 문장'을 AI가 자동으로 무한 생성하여 반복 연습을 돕는 기능.

---

## 📝 관리 및 유지보수 규칙
*   **HTML 수정 시**: `index.html`에는 오직 웹 접근성 가이드라인을 준수하는 시맨틱 구조(Semantic HTML) 및 외부 자원 연결 코드만 작성할 것. 인라인 스타일 및 로직 주입 금지.
*   **스타일 수정 시**: `style.css`에서 정의된 CSS 사용자 지정 변수(`:root` 토큰)를 기반으로 컬러와 톤앤매너를 일괄 수정할 것. ad-hoc 스타일링 지양.
*   **로직 수정 시**: 상태(State)를 변경할 경우 `localStorage` 및 `progressData` 메모리 스택에 즉각 반영하고, `saveProgress()`를 호출하여 데이터 동기화를 유지할 것.
