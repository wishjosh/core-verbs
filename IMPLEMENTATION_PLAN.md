# 📋 AI 문법 비서 & 맞춤형 문법책 상세 구현 계획서 (Implementation Plan)

이 계획서는 암기 학습 중 인공지능(AI) 튜터를 실시간 호출하여 문장별 상세 문법과 뉘앙스 차이를 분석받고, 이를 개인 맞춤형 문법 정리 노트('나만의 문법책')로 점진적으로 축적해 체화시키는 신규 기능의 상세 마크업, 스타일, 비즈니스 로직 연동 설계도입니다.

---

## 🛠️ 기능 시나리오 및 UI/UX 흐름

### 1. 학습 카드 내부 [🤖 AI 뉘앙스 과외] 버튼 도입
*   정답(영어 문장)이 노출되면 스피커 버튼 옆에 **"🤖 AI 문법·뉘앙스 분석"** 버튼이 나타납니다.
*   버튼을 누르면 AI 분석 모달 창이 열리며, 로딩 중에는 고급스러운 스켈레톤 애니메이션(Skeleton Loader)이 돌아가 학습감을 높여줍니다.

### 2. 세련된 API 연동 및 폴백(Fallback) 설계
*   **직접 API 연동 (추천)**: 사용자가 설정창(⚙️)에서 자신의 **구글 Gemini API Key** (Google AI Studio에서 무료로 즉시 발급 가능)를 한 번만 등록해 두면, 앱 외부로 나갈 필요 없이 앱 안에서 즉각적으로 AI의 답변을 받아볼 수 있습니다.
*   **폴백 설계 (API 키가 없을 때)**: API 키가 등록되지 않았을 때는, 해당 문장에 맞춤형으로 튜닝된 **프리미엄 프롬프트가 복사된 모달창**을 띄워줍니다. 사용자는 클릭 한 번으로 프롬프트를 복사해 웹 브라우저(ChatGPT 등)에 붙여넣기만 하면 최고의 답변을 얻을 수 있습니다.

### 3. 고도로 설계된 프롬프트 엔지니어링 (AI 답변 규격화)
AI에게 단순한 설명이 아닌, 나만의 문법책에 저장하기에 적합한 **완벽히 규격화된 아름다운 구조**로 답변하도록 지시합니다:
1.  **💡 문장 뼈대 분석**: 문장의 문법적 핵심 원리(시제, 전치사, 5형식 등)
2.  **🎯 미세 뉘앙스**: 왜 이 단어와 어순이 쓰였는지 다른 표현(유의어)과의 차이점 분석
3.  **🗣️ 실생활 활용 팁 & 변형 예문**: 실생활 회화에서 이 어구를 어떻게 변형해서 써먹을 수 있는지 2개 예제 추가

### 4. [📝 나만의 문법책] 탭 및 관리 시스템 추가
*   앱 최상단 타이틀 하단에 **`🃏 카드 학습`** vs **`📝 나만의 문법책`**의 미려한 슬라이딩 탭 메뉴를 추가합니다.
*   AI 분석창에서 **"💾 내 문법책에 저장"**을 누르면 해당 문장과 AI 답변이 `localStorage`의 `grammar_book` 리스트에 저장됩니다.
*   **나만의 문법책 탭 기능**:
    *   **누적 문장 리스트**: 그동안 저장한 문장들을 스크롤 형태로 제공.
    *   **상세 보기**: 클릭 시 펼쳐지는 아코디언(Accordion) 스타일로 AI 문법 가이드 확인.
    *   **✍️ 개인 메모 추가 기능**: AI 답변 아래에 내가 직접 한글로 추가적인 필기나 소감을 적어 넣을 수 있는 메모 영역 지원. (완벽한 커스텀 노트화)
    *   **정복 완료 (삭제) 버튼**: 완전히 몸에 익힌 문법은 지워서 노트를 계속 가볍고 알차게 유지.

---

## 📂 파일 구조 변경 세부 계획

기존 모듈 구조를 그대로 확장하여 코드가 길어지지 않게 구조적으로 설계합니다.

```text
/core_verbs
├── index.html      # 탭 메뉴 컨트롤러 및 AI 분석 결과 보기, 나만의 문법책 탭 추가
├── style.css       # 탭 이동 효과, AI 로딩 애니메이션, 아코디언 카드, 메모 필드 스타일
└── app.js          # Gemini API 호출, 프롬프트 주입 로직, 나만의 문법책 CRUD 제어 로직
```

---

## 🛠️ 컴포넌트별 상세 변경 사항

### 1. HTML (`index.html`)

*   **상단 헤더 바 아래 탭 네비게이션 마크업**:
    ```html
    <div class="tabs-nav">
        <button id="tab-study" class="tab-btn active" onclick="switchTab('study')">🃏 카드 학습</button>
        <button id="tab-grammar" class="tab-btn" onclick="switchTab('grammar')">📝 나만의 문법책 (<span id="grammar-count">0</span>)</button>
    </div>
    ```
*   **메인 콘텐츠 바디 분리**:
    - `#study-view` (기존의 카드 및 대시보드 영역을 감쌈)
    - `#grammar-view` (나만의 문법책 목록이 뿌려지는 신규 화면 영역, 초기 숨김 처리)
*   **AI 상세 분석 팝업 모달 (`#ai-modal`)**:
    - 로딩 스켈레톤, AI 답변 렌더러 영역, "내 문법책에 저장" 버튼 배치.
*   **설정창(`⚙️ settings-panel`) 내부**:
    - Gemini API Key를 보안 입력(비밀번호 형태)할 수 있는 인풋 필드와 AI Studio 가이드 텍스트 배치.

---

### 2. CSS (`style.css`)

*   **Tabs UI**: 둥글고 세련된 세그먼트 컨트롤러 스타일, 활성화된 탭의 배경 슬라이딩 트랜지션.
*   **AI 분석창 모달**: Glassmorphism 백드롭, 둥근 화이트 드로어 모달, 스켈레톤 로딩 바 그라데이션 애니메이션.
*   **나만의 문법책 리스트 카드**:
    - 문장 단위의 리드미컬한 배치 및 펼쳐보기 아코디언 디자인.
    - 개인 메모를 적는 귀여운 편지지 테마의 노란색 손글씨풍 `<textarea>` 메모 박스 디자인.
    - 정복 완료 배지 및 마우스 오버 시 미세 들림 효과.

---

### 3. JavaScript (`app.js`)

#### 1. Gemini API 연동 모듈 탑재:
```javascript
// 프롬프트 가이드라인 설계
const SYSTEM_PROMPT = `당신은 영어 학습자를 위한 전담 원어민 튜터입니다. 
제공된 영어 문장과 한글 번역에 대해 문법적 특징과 원어민 뉘앙스를 다음 3가지 항목으로 아주 명확하고 친절하게 한국어로 설명해 주세요:
1. 💡 문법 핵심 원리 (쉽게 이해되는 뼈대 분석)
2. 🎯 원어민 뉘앙스 차이 (왜 이 단어/시제가 쓰였는지, 다른 비슷한 표현과 어떻게 느낌이 다른지)
3. 🗣️ 실생활 100% 활용 응용 예문 2가지 (실제 원어민들이 일상에서 바로 쓸 수 있는 생생한 영어 대화 예시)

마크업 가독성을 위해 항목마다 이모지를 적극 사용하고, 중요 부분은 볼드체(**)로 표기해 주세요.`;
```
*   `fetchGeminiResponse(englishText, koreanText)`: API 키를 읽어와 구글 제미나이(Flash 2.5/3 API) 엔드포인트로 전송, 마크다운 형태의 답변 렌더링.

#### 2. 데이터 저장 스키마 추가:
*   `localStorage`에 `coreVerbs_GrammarBook_v1` 키를 활용해 `{ id, day, verb, en, ko, aiResponse, userMemo, date }` 배열 저장.
*   학습 카드 내 **"💾 내 문법책에 추가"** 클릭 시 중복 체크 후 리스트에 삽입 및 로컬 스토리지에 동기화.

#### 3. 문법책 탭의 CRUD 및 동적 렌더링 기능:
*   `renderGrammarBook()`: 저장된 데이터를 읽어와 카드 형태로 드로잉.
*   `toggleGrammarDetail(id)`: AI 답변 영역 펼치기/접기 제어.
*   `updateUserMemo(id, text)`: 사용자가 텍스트 박스에 필기한 나만의 감상/메모를 실시간 `localStorage`에 자동 저장 (디바운싱 기술 적용해 타자 타이핑 중 멈추면 세이브되도록 최적화).
*   `deleteGrammarCard(id)`: 문법책에서 완료 카드 삭제 기능.

---

## 🧪 검증 및 구현 순서 계획

1.  **UI 뼈대 수립**: `index.html`에 탭 내비게이션, 문법책 뷰, AI 대화 모달 마크업 배치 및 스타일 추가.
2.  **API 호출 모듈 구현**: 설정창 API 키 저장 기능 및 프롬프트 주입 테스트. 키가 없을 때의 클립보드 복사 폴백 로직 확인.
3.  **학습 로직 연동**: 정답 카드에서 AI 버튼을 눌렀을 때 해당 카드의 한국어/영어 문장이 프롬프트에 자동으로 합쳐져 날아가는지 검증.
4.  **문법책 데이터 구조화**: 문법책에 저장을 눌렀을 때 메모리 누수 없이 스토리지에 세이브되는지 확인.
5.  **나만의 메모장 튜닝**: 직접 손글씨 메모를 적었을 때 새로고침 후에도 글이 지워지지 않고 깔끔하게 복원되는지 완벽 테스트.
